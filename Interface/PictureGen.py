class PictureGen:
    
    def genPyramid(cards: []) -> pic:
        """
        To generate the Pyramid.

        Parameters
        cards: (len(cards) == 10) && (type(cards[0]) == str) 
        ['00','00','00','00','00','00','00','00','00','00']
        """
        pass

    def genHand(cards: []) -> pic:
        """
        To generate the hand cards.

        Parameters
        cards: (len(cards) == 2) && (type(cards[0]) == str) 
        ['00','00']
        """
        pass

    def genBus(cards: []) -> pic:
        """
        To generate the Busfahrer MiniGame.

        Parameters
        cards: (len(cards) == 5) && (type(cards[0]) == str) 
        ['00','00','00','00','00']
        """
        pass